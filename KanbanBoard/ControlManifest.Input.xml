<?xml version="1.0" encoding="utf-8" ?>
<manifest>
  <!--
    The version here is what the Power Platform upgrades on, and the release
    workflow refuses to publish a tag that disagrees with it. Bump this in the
    same commit as the tag.
  -->
  <control namespace="PCFHub"
           constructor="KanbanBoard"
           version="0.2.1"
           display-name-key="KanbanBoard_Name"
           description-key="KanbanBoard_Desc"
           control-type="virtual">

    <external-service-usage enabled="false" />

    <!--
      Roles, deliberately — the opposite decision from pcf-data-table and
      pcf-compact-list, and for the reason the template gives: this control
      assigns meaning to specific columns, the way a chip has a label and a
      colour. A card has a lane, a headline, and optionally a person and a
      badge. It does not render "whatever the view supplies", so there is
      nothing for it to read off dataset.columns generically.

      The arity is fixed here permanently. Roles can be added in a later
      version; they cannot be removed without breaking every maker who bound
      one. Four is the considered ceiling: statusField and titleField are what
      a board *is*, and the other two are the difference between a usable card
      and a coloured rectangle.

      Found on the column by `alias`, read off the record by `name`. Getting
      that backwards renders nothing at all against a real view while passing a
      fixture whose alias and name are the same string — which is why
      demo/records.json here uses realistic, different values.
    -->
    <data-set name="records" display-name-key="Records_DataSet_Name">
      <!--
        The lane the card sits in, and the only column this control writes.

        of-type="OptionSet" rather than a text column: the lanes are the
        option set's values, and moving a card writes the option's *number*.
        Binding this to text would make the write meaningless.
      -->
      <property-set name="statusField"
                    display-name-key="StatusField_Name"
                    description-key="StatusField_Desc"
                    of-type="OptionSet"
                    usage="bound"
                    required="true" />

      <property-set name="titleField"
                    display-name-key="TitleField_Name"
                    description-key="TitleField_Desc"
                    of-type="SingleLine.Text"
                    usage="bound"
                    required="true" />

      <property-set name="assigneeField"
                    display-name-key="AssigneeField_Name"
                    description-key="AssigneeField_Desc"
                    of-type="SingleLine.Text"
                    usage="bound"
                    required="false" />

      <property-set name="badgeField"
                    display-name-key="BadgeField_Name"
                    description-key="BadgeField_Desc"
                    of-type="SingleLine.Text"
                    usage="bound"
                    required="false" />
    </data-set>

    <!--
      No `cds-data-set-options`, following the template's own rule: a control
      with fixed roles wants the subgrid chrome off, because its layout does not
      survive the maker picking a different view. The command bar would also
      have nothing to act on — this control never calls setSelectedRecordIds().
    -->

    <!--
      The lanes, when deriving them is not enough.

      Left empty, the lanes are the distinct values actually present in the
      bound records — which is right most of the time and cannot invent a lane
      that no record is in. An empty "Done" column is exactly what a board
      needs to show, so this is the escape hatch: "1=New,2=Active,3=Resolved"
      fixes both the set and the order.

      Left empty the board now reads the option set through
      context.utils.getEntityMetadata, so every lane appears including the empty
      ones and a new board is usable without configuring anything. This property
      is the override: fewer lanes than the column has, a different order, or
      labels of your own.

      That metadata call was rejected once, on the grounds that it costs a
      second install-time permission prompt and does not exist in canvas. The
      second half was wrong. WebAPI does not exist in canvas either, so the
      board is read-only there whatever this control does — declining the call
      bought canvas nothing and cost model-driven the behaviour that makes the
      board work at all. See SPEC.md.
    -->
    <property name="lanes"
              display-name-key="Lanes_Name"
              description-key="Lanes_Desc"
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="laneWidth"
              display-name-key="LaneWidth_Name"
              description-key="LaneWidth_Desc"
              of-type="Whole.None"
              usage="input"
              required="false"
              default-value="280" />

    <!--
      Colour comes from the option set, so it exists only where the option set
      was read: a model-driven board. Derived lanes and lanes named in the
      `lanes` override have none, and simply show no bar.

      On by default, with this to turn it off, because Dataverse assigns option
      colours automatically when a choice is created — the colours are usually
      present whether or not anybody chose them, and a board where they are
      noise should be able to say so.
    -->
    <property name="laneColors"
              display-name-key="LaneColors_Name"
              description-key="LaneColors_Desc"
              of-type="TwoOptions"
              usage="input"
              required="false"
              default-value="true" />

    <property name="openOnCardClick"
              display-name-key="OpenOnCardClick_Name"
              description-key="OpenOnCardClick_Desc"
              of-type="TwoOptions"
              usage="input"
              required="false"
              default-value="true" />

    <!--
      A board loads more, it does not turn pages, so this is how many cards
      arrive per fetch rather than how many are on screen. The platform clamps
      large values.
    -->
    <!--
      NO `default-value` HERE, AND THAT IS THE DECISION RATHER THAN AN OMISSION.

      **A `default-value` on an optional property is not a default, it is an
      override.** The platform hands the value down whether or not the maker ever
      touched the property, so there is no unset state left to express — and this
      property mirrors something the host has already decided.
      `dataset.paging.pageSize` is the size the host is actually retrieving with:
      on a main grid the user's own *Rows per page* personalisation, on a subgrid
      what the maker set in the form designer, in canvas the platform default.

      `pcf-row-commands` shipped `default-value="25"` and replaced all three on
      every install. Unset, this control adopts what the platform is already doing
      and never calls `setPageSize`; set, it overrides.

      Only the types carrying a null can say this at all — `Whole.None` is
      `number | null`, which is exactly what a `TwoOptions` is not.
    -->
    <property name="pageSize"
              display-name-key="PageSize_Name"
              description-key="PageSize_Desc"
              of-type="Whole.None"
              usage="input"
              required="false" />

    <!--
      Two outputs, because "which card moved" and "which card was opened" are
      different questions a form reacts to differently.

      Both are notified *before* the platform call they describe, so the value
      is observable even on a host where the call does nothing — the canvas
      case for openDatasetItem, and the failure case for updateRecord.
    -->
    <property name="movedRecordId"
              display-name-key="MovedRecordId_Name"
              description-key="MovedRecordId_Desc"
              of-type="SingleLine.Text"
              usage="output" />

    <property name="openedRecordId"
              display-name-key="OpenedRecordId_Name"
              description-key="OpenedRecordId_Desc"
              of-type="SingleLine.Text"
              usage="output" />

    <resources>
      <code path="index.ts" order="1" />
      <css path="css/KanbanBoard.css" order="1" />
      <platform-library name="React" version="16.14.0" />
      <platform-library name="Fluent" version="9.46.2" />
      <resx path="strings/KanbanBoard.1033.resx" version="1.0.0" />
      <resx path="strings/KanbanBoard.3082.resx" version="1.0.0" />
      <resx path="strings/KanbanBoard.1036.resx" version="1.0.0" />
      <resx path="strings/KanbanBoard.1031.resx" version="1.0.0" />
      <resx path="strings/KanbanBoard.1041.resx" version="1.0.0" />
    </resources>

    <!--
      WebAPI only, and it is genuinely used: moving a card calls
      updateRecord(). That is one install-time permission prompt this control
      has earned, and the reason the lanes are derived rather than read through
      Utility — a second prompt for something a text property can answer is not
      a trade worth making.

      **required="false", and that is the whole difference between a board that
      degrades and one that will not start.**

      WebAPI is a Dataverse-dependent API and is not available in canvas apps
      (Power Apps component framework "Limitations"). The documented behaviour
      of required="true" on a host that does not support the feature is not a
      null accessor to check — it is a design-time warning and *component load
      failure at runtime* (manifest schema reference, uses-feature). So
      required="true" would mean a board that renders nothing at all in a canvas
      app rather than one that shows the cards and declines to move them.

      Reading a view is most of what this control does. With required="false"
      the host leaves context.webAPI absent instead of refusing to load, index.ts
      feature-detects it, and canvas gets a read-only board. Moving a card needs
      a model-driven app, or a published custom page — custom pages have runtime
      WebAPI support, though the studio preview reports "Method not implemented".

      openDatasetItem() needs no feature: it is a method on the dataset itself,
      not on context.navigation.
    -->
    <feature-usage>
      <uses-feature name="WebAPI" required="false" />
      <uses-feature name="Utility" required="false" />
    </feature-usage>
  </control>
</manifest>
