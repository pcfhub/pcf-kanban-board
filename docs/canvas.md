---
title: Canvas apps
description: Adding Kanban Board to a canvas app or custom page.
order: 3
---

# Using it in a canvas app

<!--
  Delete this page if the control does not support canvas apps. An empty section
  on the hub is worse than a missing one — it reads as unfinished rather than
  as deliberate.
-->

:::steps
1. From **Insert → Get more components**, open the **Code** tab and import
   **Kanban Board**.
2. Place it from **Insert → Code components**.
3. Bind the properties below.
:::

## Wiring the properties

```powerfx
Set(varValue, "Hello");
```

| Property | Value |
| --- | --- |
| Value | `varValue` |
| Placeholder | `"Type here"` |

## Reading the output

Explain what the control writes back and when — `OnChange`, an output property,
or an event — with the formula a maker would actually paste.
